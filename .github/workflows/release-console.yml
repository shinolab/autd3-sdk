name: release-console

on:
  push:
    tags:
      - 'console-v*'

defaults:
  run:
    shell: bash

permissions:
  contents: write

jobs:
  release:
    name: release-console
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: ./.github/actions/setup-rust
      - uses: taiki-e/install-action@v2
        with:
          tool: git-cliff

      - name: Generate release notes
        run: cargo xtask changelog --tag "${GITHUB_REF_NAME}" --release-notes --output RELEASE_NOTES.md

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release create "${GITHUB_REF_NAME}" --title "${GITHUB_REF_NAME}" --notes-file RELEASE_NOTES.md

  bundle:
    name: console-bundle (${{ matrix.os }})
    needs: release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      - uses: ./.github/actions/setup-rust
        with:
          targets: wasm32-unknown-unknown
          workspaces: |
            console
            simulator
            simulator/frontend
            xtask
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-binstall,cargo-about
      - name: Install dioxus-cli
        run: cargo binstall -y dioxus-cli --locked
      - name: Build bundle
        run: cargo xtask console bundle
      - name: Upload bundle
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${GITHUB_REF_NAME}" console/target/bundle/autd3-console-*.tar.gz
