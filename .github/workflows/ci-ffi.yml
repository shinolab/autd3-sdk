name: ci-ffi

on:
  workflow_call:

defaults:
  run:
    shell: bash

jobs:
  ffi:
    name: ffi (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-rust
        with:
          components: rustfmt, clippy
          workspaces: |
            bindings/ffi
            xtask
          shared-key: ffi
      - name: Install system deps
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libpcap-dev
      - name: Format
        if: runner.os == 'Linux'
        run: cargo xtask ffi format
      - run: cargo xtask ffi lint
      - run: cargo xtask ffi test
      - run: cargo xtask ffi build
