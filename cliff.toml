[changelog]
header = """
# Changelog

"""
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}
{% for commit in commits %}
- {% if commit.scope %}*({{ commit.scope }})* {% endif %}\
{% if commit.breaking %}[**breaking**] {% endif %}\
{{ commit.message | upper_first }}\
{% endfor %}
{% endfor %}\n
"""
footer = ""
trim = true

[git]
conventional_commits = true
filter_unconventional = true
split_commits = false
commit_parsers = [
    { footer = "^BREAKING[ -]CHANGE", group = "<!-- 0 -->💥 Breaking Changes" },
    { message = "^[a-z]+(\\(.+\\))?!", group = "<!-- 0 -->💥 Breaking Changes" },
    { message = "^feat", group = "<!-- 1 -->🚀 Features" },
    { message = "^fix", group = "<!-- 2 -->🐛 Bug Fixes" },
    { message = "^perf", group = "<!-- 3 -->⚡ Performance" },
    { message = "^revert", group = "<!-- 4 -->◀️ Revert" },
    { message = "^docs", skip = true },
    { message = "^refactor", skip = true },
    { message = "^test", skip = true },
    { message = "^chore\\(deps\\)", group = "<!-- 8 -->📦 Dependencies" },
    { message = "^chore", skip = true },
    { message = "^build", skip = true },
    { message = "^ci", skip = true },
    { message = ".*", skip = true },
]
filter_commits = true
topo_order = false
sort_commits = "oldest"
# Default for standalone `git-cliff` runs. xtask overrides this per release
# component via `--tag-pattern` (see xtask `component.rs`).
tag_pattern = "v[0-9]*"
