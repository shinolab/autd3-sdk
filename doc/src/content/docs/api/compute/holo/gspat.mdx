---
title: gspat
description: GS-PAT 法による多焦点最適化
sidebar:
  order: 13
srcPath: crates/autd3-rs-pattern-holo/src/linear_synthesis/gspat.rs
---

import CodeTabs from "@components/CodeTabs.astro";
import sample from "@codes/rust/examples/api/compute/holo/gspat.rs?raw";
import pySample from "@codes/python/examples/api/compute/holo/gspat.py?raw";
import fullSample from "@codes/rust/examples/api/compute/holo/gspat-example.rs?raw";
import pyFullSample from "@codes/python/examples/api/compute/holo/gspat-example.py?raw";
import { excerpt } from "@lib/excerpt";

gspat は, [gs](/autd3-sdk/api/compute/holo/gs/) を高速化したアルゴリズム.
アルゴリズムは Plasencia らの論文 ([Plasencia et al., 2020](https://doi.org/10.1145/3386569.3392492)) に基づく.

## API

<CodeTabs rust={excerpt(sample, "api")} python={excerpt(pySample, "api")} />

| 引数         | 型                     | 説明                      |
| ------------ | ---------------------- | ------------------------- |
| `geometry`   | `&Geometry`            | デバイス配置              |
| `foci`       | `&[ControlPoint]`      | 焦点列 (位置と目標振幅)   |
| `wavelength` | `Length`               | 波長                      |
| `option`     | `&GspatOption`         | 追加オプション (下記参照) |
| `out`        | `&mut [[Emission; _]]` | 出力バッファ              |

引数は [gs](/autd3-sdk/api/compute/holo/gs/) と同一. 

## GspatOption

<CodeTabs rust={excerpt(sample, "option")} python={excerpt(pySample, "option")} />

| フィールド    | 型                   | デフォルト                    |
|---------------|----------------------|---------------------------|
| `repeat`      | `NonZeroUsize`       | `100`                     |
| `constraint`  | `EmissionConstraint` | `Clamp(MIN, MAX)`         |
| `directivity` | `Directivity`        | `Sphere`                  |
| `backend`     | `impl LinAlgBackend` | `NalgebraBackend`         |
| `mask`        | `TransducerMask`     | `AllEnabled`              |

オプションは [gs](/autd3-sdk/api/compute/holo/gs/) と同一.

## Example

<CodeTabs rust={excerpt(fullSample)} python={excerpt(pyFullSample)} />

