---
title: hermite_gaussian
description: エルミート・ガウシアンビームを作る Pattern
sidebar:
  order: 6
srcPath: crates/autd3-rs-pattern/src/hermite_gaussian.rs
---

import CodeTabs from "@components/CodeTabs.astro";
import Math from "@components/Math.astro";
import MathBlock from "@components/MathBlock.astro";
import PatternField from "@components/PatternField.astro";
import sample from "@codes/rust/examples/api/compute/pattern/hermite-gaussian.rs?raw";
import pySample from "@codes/python/examples/api/compute/pattern/hermite-gaussian.py?raw";
import fullSample from "@codes/rust/examples/api/compute/pattern/hermite-gaussian-example.rs?raw";
import pyFullSample from "@codes/python/examples/api/compute/pattern/hermite-gaussian-example.py?raw";
import csSample from "@codes/csharp/examples/api/compute/pattern/hermite-gaussian.cs?raw";
import csFullSample from "@codes/csharp/examples/api/compute/pattern/hermite-gaussian-example.cs?raw";
import { excerpt } from "@lib/excerpt";

`hermite_gaussian_phase` はエルミート・ガウシアン (HG) ビーム <Math tex="\mathrm{HG}_{mn}" /> を生成する.

<PatternField kind="hermite-gaussian" />

`laguerre_gaussian_phase` と同様に, 振幅は `hermite_gaussian_intensity` で変更する.

## API

<CodeTabs rust={excerpt(sample, "api")} python={excerpt(pySample, "api")} csharp={excerpt(csSample, "api")} />

| 引数         | 型                      | 説明                                          |
| ------------ | ----------------------- | --------------------------------------------- |
| `geometry`   | `&Geometry`             | デバイス配置                                  |
| `target`     | `Point3<f32>`           | ビームウエストの中心座標 [mm]                 |
| `axis`       | `UnitVector3<f32>`      | ビームの伝搬方向 (単位ベクトル)               |
| `x_dir`      | `UnitVector3<f32>`      | 次数 `m` の節面を並べる方向                   |
| `option`     | `HermiteGaussianOption` | モードとウエスト                              |
| `wavelength` | `Length`                | 波長                                          |
| `dst`        | `&mut [Vec<Phase>]` / `&mut [Vec<Intensity>]` | 出力バッファ (`*_phase` は位相, `*_intensity` は振幅) |

| フィールド | 型       | 説明                                                  |
| ---------- | -------- | ----------------------------------------------------- |
| `m`        | `u32`    | `x_dir` 方向のモード次数                              |
| `n`        | `u32`    | それに直交する方向のモード次数                        |
| `waist`    | `Length` | ビームウエスト半径 <Math tex="w_0" /> (正値) |

## Example

<CodeTabs rust={excerpt(fullSample)} python={excerpt(pyFullSample)} csharp={excerpt(csFullSample)} />
