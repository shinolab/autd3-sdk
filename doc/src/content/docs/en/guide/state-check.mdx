---
title: Link State Monitoring
description: Monitor the EtherCAT link state of devices with the State Checker
sidebar:
  order: 6
---

import CodeTabs from "@components/CodeTabs.astro";
import { excerpt } from "@lib/excerpt";
import sample from "@codes/rust/examples/guide/state-check.rs?raw";
import pySample from "@codes/python/examples/guide/state-check.py?raw";
import csSample from "@codes/csharp/examples/guide/state-check.cs?raw";

There are cases where you want to monitor whether the EtherCAT link to the device is healthy during operation.
`Client` does not spawn a background task to monitor the state on its own.
Instead, it returns a `Checker` that the user drives at their own timing.
The user has full control over the monitoring frequency and the response to state changes.

## Obtaining the Checker

Using `open_with_checker` instead of the usual [`Client::open`](/autd3-sdk/en/api/basics/client/) returns a pair of `Client` and `Checker`.

<CodeTabs rust={excerpt(sample, "open")} python={excerpt(pySample, "open")} csharp={excerpt(csSample, "open")} />

## Obtaining the State

`Checker::check` returns the link state `LinkStatus` at the time of the call.
`LinkStatus` holds the following.

- `devices`: the state of each device (`DeviceState`). `OP` when normal, `LOST` when disconnected, etc.
- `all_op`: whether all devices are in the `OP` state.
- `any_lost`: whether any device is in the `LOST` state.
- `recoveries`: the cumulative number of recoveries from disconnection.

The following is an example that calls `check` at fixed intervals and outputs only when the state changes.

<CodeTabs rust={excerpt(sample, "poll")} python={excerpt(pySample, "poll")} csharp={excerpt(csSample, "poll")} />

How often to call `check` is up to the user.
A typical usage is to poll at a low frequency as above, in a task separate from the high-frequency send loop.
