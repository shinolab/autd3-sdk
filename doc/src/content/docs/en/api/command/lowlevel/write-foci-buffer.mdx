---
title: WriteFociBuffer
description: Writing to the Foci STM buffer
sidebar:
  order: 18
---

import CodeTabs from "@components/CodeTabs.astro";
import sample from "@codes/rust/examples/api/command/lowlevel/foci-buffer.rs?raw";
import pySample from "@codes/python/examples/api/command/lowlevel/foci-buffer.py?raw";
import fullSample from "@codes/rust/examples/api/command/lowlevel/foci-buffer-example.rs?raw";
import pyFullSample from "@codes/python/examples/api/command/lowlevel/foci-buffer-example.py?raw";
import csSample from "@codes/csharp/examples/api/command/lowlevel/foci-buffer.cs?raw";
import csFullSample from "@codes/csharp/examples/api/command/lowlevel/foci-buffer-example.cs?raw";
import { excerpt } from "@lib/excerpt";

`WriteFociBuffer` is a low-level command that writes a focal point sequence for Foci STM into a bank.

## API

<CodeTabs rust={excerpt(sample, "write")} python={excerpt(pySample, "write")} csharp={excerpt(csSample, "write")} />

| Field          | Type                  | Description                        |
| -------------- | --------------------- | ---------------------------------- |
| `bank`         | `PatternBank`         | Destination bank                   |
| `index_offset` | `usize`               | Start index for writing within the bank |
| `points`       | `&[ControlPoints<N>]` | Sequence of focal point samples    |

`ControlPoints<N>` is one sample holding `N` focal points (`ControlPoint`) and an amplitude.
For details see [FociStm](/autd3-sdk/en/api/command/foci-stm/#controlpoint--controlpoints).

## Example

An example of playing a focal point sequence as Foci STM via `WriteFociBuffer` → `ConfigFociStm` → `ChangePatternBank` (equivalent to the high-level [FociStm](/autd3-sdk/en/api/command/foci-stm/)).

<CodeTabs rust={excerpt(fullSample)} python={excerpt(pyFullSample)} csharp={excerpt(csFullSample)} />
