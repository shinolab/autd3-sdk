---
title: Clear
description: Command to clear the device state
sidebar:
  order: 10
---

import CodeTabs from "@components/CodeTabs.astro";
import sample from "@codes/rust/examples/api/command/clear.rs?raw";
import pySample from "@codes/python/examples/api/command/clear.py?raw";
import fullSample from "@codes/rust/examples/api/command/clear-example.rs?raw";
import pyFullSample from "@codes/python/examples/api/command/clear-example.py?raw";
import csSample from "@codes/csharp/examples/api/command/clear.cs?raw";
import csFullSample from "@codes/csharp/examples/api/command/clear-example.cs?raw";
import { excerpt } from "@lib/excerpt";

`Clear` is a command that returns the device banks and various settings to the state right after startup.
All settings such as Silencer / Pattern / Modulation are reinitialized.

## API

<CodeTabs rust={excerpt(sample, "api")} python={excerpt(pySample, "api")} csharp={excerpt(csSample, "api")} />

## Example

<CodeTabs rust={excerpt(fullSample)} python={excerpt(pyFullSample)} csharp={excerpt(csFullSample)} />
