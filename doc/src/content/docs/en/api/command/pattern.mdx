---
title: Pattern
description: Command that sends a computed phase and intensity pattern to the device
sidebar:
  order: 1
---

import CodeTabs from "@components/CodeTabs.astro";
import { Code } from "astro-expressive-code/components";
import sample from "@codes/rust/examples/api/command/pattern.rs?raw";
import pySample from "@codes/python/examples/api/command/pattern.py?raw";
import fullSample from "@codes/rust/examples/api/command/pattern-example.rs?raw";
import pyFullSample from "@codes/python/examples/api/command/pattern-example.py?raw";
import csSample from "@codes/csharp/examples/api/command/pattern.cs?raw";
import csFullSample from "@codes/csharp/examples/api/command/pattern-example.cs?raw";
import { excerpt } from "@lib/excerpt";

`Pattern` is a command that sends the phase and intensity of each transducer to the device.
The phases and intensities are passed as separate buffers.

## API

<CodeTabs>
<Fragment slot="rust">
<Code code={excerpt(sample, "api")} lang="rust" />

`Pattern::new` sets the destination bank to `PatternBank::B0`.
To write to another bank, use `Pattern::with_bank(bank, phases, intensities)`.
</Fragment>
<Fragment slot="python">
<Code code={excerpt(pySample, "api")} lang="python" />

`Pattern` sets the destination bank to `PatternBank.B0` by default.
To write to another bank, use `Pattern(phases, intensities, bank=bank)`.
</Fragment>
<Fragment slot="csharp">
<Code code={excerpt(csSample, "api")} lang="csharp" />

`new Pattern(phases, intensities)` sets the destination bank to `PatternBank.B0`.
To write to another bank, use `new Pattern(bank, phases, intensities)`.
</Fragment>
</CodeTabs>

| Field             | Type                             | Default                     | Description                      |
| ----------------- | -------------------------------- | --------------------------- | -------------------------------- |
| `bank`            | `PatternBank`                    | `PatternBank::B0`           | Bank to write to / play back     |
| `phases`          | `&[Vec<Phase>]`     | —                           | Phases per device                |
| `intensities`     | `PatternIntensity`  | —                           | Intensity (a uniform `Intensity`, or a per-device `&[Vec<Intensity>]` buffer) |
| `transition_mode` | `TransitionMode`                 | `TransitionMode::Immediate` | Behavior when switching the bank |

Several [helper functions](/autd3-sdk/en/api/compute/pattern/) are provided for computing patterns.

## Equivalent Low-Level Commands

`Pattern` is equivalent to the three commands [WritePatternBuffer](/autd3-sdk/en/api/command/lowlevel/write-pattern-buffer/) + [ConfigPattern](/autd3-sdk/en/api/command/lowlevel/config-pattern/) + [ChangePatternBank](/autd3-sdk/en/api/command/lowlevel/change-pattern-bank/), which are fused into a single frame.
The following is equivalent to `Pattern { bank, phases, intensities }`.

When `transition_mode` is `TransitionMode::Later`, nothing is fused: the command decomposes into the two frames `WritePatternBuffer` + `ConfigPattern` and no `ChangePatternBank` is sent.
See the `TransitionMode` section of [ChangePatternBank](/autd3-sdk/en/api/command/lowlevel/change-pattern-bank/) for details.

<CodeTabs rust={excerpt(sample, "equivalent")} python={excerpt(pySample, "equivalent")} csharp={excerpt(csSample, "equivalent")} />

## Example

<CodeTabs rust={excerpt(fullSample)} python={excerpt(pyFullSample)} csharp={excerpt(csFullSample)} />
