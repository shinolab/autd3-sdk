---
title: sine
description: Modulation that generates a sine-wave amplitude modulation
sidebar:
  order: 21
srcPath: crates/autd3-rs-modulation/src/sine.rs
---

import Math from "@components/Math.astro";
import MathBlock from "@components/MathBlock.astro";
import CodeTabs from "@components/CodeTabs.astro";
import ModulationPlot from "@components/ModulationPlot.astro";
import sample from "@codes/rust/examples/api/compute/modulation/sine.rs?raw";
import pySample from "@codes/python/examples/api/compute/modulation/sine.py?raw";
import fullSample from "@codes/rust/examples/api/compute/modulation/sine-example.rs?raw";
import pyFullSample from "@codes/python/examples/api/compute/modulation/sine-example.py?raw";
import csSample from "@codes/csharp/examples/api/compute/modulation/sine.cs?raw";
import csFullSample from "@codes/csharp/examples/api/compute/modulation/sine-example.cs?raw";
import { excerpt } from "@lib/excerpt";

`sine` generates a sine-wave amplitude modulation.

<ModulationPlot fixed="freq = 200 Hz (clamp = true)" />

## API

<CodeTabs rust={excerpt(sample, "api")} python={excerpt(pySample, "api")} csharp={excerpt(csSample, "api")} />

| Parameter | Type           | Description                                  |
| --------- | -------------- | -------------------------------------------- |
| `freq`    | `Freq<u32>` etc. | modulation frequency                       |
| `option`  | `&SineOption`  | additional options (see below)               |
| `dst`     | `&mut Vec<u8>` | output buffer                                |

The value of each sample <Math tex="i" /> is given by the following equation, for amplitude <Math tex="A" /> (`amplitude`), offset <Math tex="b" /> (`offset`), and phase <Math tex="\theta" /> (`phase`).

<MathBlock tex="m_i = \frac{A}{2}\sin\!\left(2\pi f t_i + \theta\right) + b" />

`freq` is specified as `Freq<u32>` (`150 * Hz`) or `Freq<f32>` (`150.0 * Hz`).

## SineOption

<CodeTabs rust={excerpt(sample, "option")} python={excerpt(pySample, "option")} csharp={excerpt(csSample, "option")} />

| Field             | Type             | Default                 | Description                                              |
| ----------------- | ---------------- | ----------------------- | -------------------------------------------------------- |
| `amplitude`       | `u8`             | `0xFF`                  | amplitude of the sine wave                               |
| `offset`          | `u8`             | `0x80`                  | DC offset                                                |
| `phase`           | `Angle`          | `Angle::ZERO`           | phase                                                    |
| `clamp`           | `bool`           | `false`                 | whether to clamp out-of-range values to [0, 255]         |
| `sampling_config` | `SamplingConfig` | `SamplingConfig::FREQ_4K` | sampling configuration                                 |

If the combination of `amplitude` and `offset` causes the waveform to fall outside the [0, 255] range, an error occurs when `clamp` is `false`.
Choose values that stay within range, or set `clamp` to `true`.

## Constraints

Due to the [constraints of SamplingConfig](/autd3-sdk/en/api/basics/sampling-config/), the specified frequency may not be a representable frequency.
In that case, passing `Nearest(freq)` rounds it to the nearest representable frequency.

<CodeTabs rust={excerpt(sample, "nearest")} python={excerpt(pySample, "nearest")} csharp={excerpt(csSample, "nearest")} />

## Example

<CodeTabs rust={excerpt(fullSample)} python={excerpt(pyFullSample)} csharp={excerpt(csFullSample)} />

