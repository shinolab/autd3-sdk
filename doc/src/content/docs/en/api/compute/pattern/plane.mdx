---
title: plane
description: Pattern that generates a plane wave
sidebar:
  order: 3
srcPath: crates/autd3-rs-pattern/src/plane.rs
---

import CodeTabs from "@components/CodeTabs.astro";
import PatternField from "@components/PatternField.astro";
import sample from "@codes/rust/examples/api/compute/pattern/plane.rs?raw";
import pySample from "@codes/python/examples/api/compute/pattern/plane.py?raw";
import fullSample from "@codes/rust/examples/api/compute/pattern/plane-example.rs?raw";
import pyFullSample from "@codes/python/examples/api/compute/pattern/plane-example.py?raw";
import csSample from "@codes/csharp/examples/api/compute/pattern/plane.cs?raw";
import csFullSample from "@codes/csharp/examples/api/compute/pattern/plane-example.cs?raw";
import { excerpt } from "@lib/excerpt";

`plane` generates a plane wave propagating in the specified direction.

<PatternField kind="plane" />

## API

<CodeTabs rust={excerpt(sample, "api")} python={excerpt(pySample, "api")} csharp={excerpt(csSample, "api")} />

| Parameter    | Type                   | Description                          |
| ------------ | ---------------------- | ----------------------------------- |
| `geometry`   | `&Geometry`            | Device layout                       |
| `direction`  | `UnitVector3<f32>`     | Propagation direction of the plane wave (unit vector) |
| `wavelength` | `Length`               | Wavelength                          |
| `dst`        | `&mut [[Emission; _]]` | Output buffer                       |

## Example

<CodeTabs rust={excerpt(fullSample)} python={excerpt(pyFullSample)} csharp={excerpt(csFullSample)} />
