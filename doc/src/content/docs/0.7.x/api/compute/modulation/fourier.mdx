---
title: fourier
description: 複数の正弦波を合成した振幅変調を生成する Modulation
sidebar:
  order: 23
srcPath: crates/autd3-rs-modulation/src/fourier.rs
slug: 0.7.x/api/compute/modulation/fourier
---

import Math from "@components/Math.astro";
import MathBlock from "@components/MathBlock.astro";
import CodeTabs from "@components/CodeTabs.astro";

`fourier` は複数の正弦波成分を合成した振幅変調を生成する. 各成分を [sine](/autd3-sdk/0.7.x/api/compute/modulation/sine/) と同じ方法で計算し, 足し合わせて 1 つの波形にする.

## API

<CodeTabs rust={"fourier(&components, &option, &mut dst)?;"} python={"fourier(components, option, dst)"} csharp={"Modulation.Fourier(components, option, dst);"} />

| 引数         | 型                    | 説明                      |
| ------------ | --------------------- | ------------------------- |
| `components` | `&[SineComponent<S>]` | 合成する正弦波成分の配列  |
| `option`     | `&FourierOption`      | 追加オプション (下記参照) |
| `dst`        | `&mut Vec<u8>`        | 出力バッファ              |

各 `SineComponent` は周波数 `freq` と `SineOption` の組である.
全成分の `sampling_config` は一致している必要があり, 異なるとエラーになる.
合成波形の長さは各成分の周期の最小公倍数である.

各サンプルは全成分の和にスケール <Math tex="s" /> (`scale_factor`) を掛け, オフセット <Math tex="b" /> (`offset`) を加えた値である.

<MathBlock tex="m_i = s \sum_{k} m^{(k)}_i + b" />

## SineComponent

| フィールド | 型             | 説明                    |
| ---------- | -------------- | ----------------------- |
| `freq`     | `Freq<u32>` 等 | 成分の周波数            |
| `option`   | `SineOption`   | 成分ごとの正弦波 Option |

<CodeTabs rust={"SineComponent {\n    freq: 100 * Hz,\n    option: SineOption::default(),\n};"} python={"SineComponent(\n    100.0 * Hz,\n    SineOption(),\n)"} csharp={"new SineComponent(\n    100 * Hz,\n    new SineOption()\n);"} />

## FourierOption

<CodeTabs rust={"FourierOption {\n    scale_factor,\n    clamp,\n    offset,\n    ..Default::default()\n}"} python={"FourierOption(\n    scale_factor,\n    clamp,\n    offset,\n)"} csharp={"new FourierOption(\n    scaleFactor,\n    clamp,\n    offset\n)"} />

| フィールド     | 型            | デフォルト | 説明                                                      |
| -------------- | ------------- | ---------- | --------------------------------------------------------- |
| `scale_factor` | `Option<f32>` | `None`     | 合成値に掛けるスケール. `None` 場合, 成分数の逆数         |
| `clamp`        | `bool`        | `false`    | 範囲外の値を \[0, 255] clampするか                         |
| `offset`       | `u8`          | `0`        | 合成後に加える直流オフセット                              |

## Example

<CodeTabs rust={"use autd3_rs::units::Hz;\nuse autd3_rs_modulation::{FourierOption, SineComponent, SineOption, fourier};\n\nlet mut dst = Vec::new();\n\nfourier(\n    &[SineComponent {\n        freq: 100 * Hz,\n        option: SineOption::default(),\n    }],\n    &FourierOption {\n        scale_factor: None,\n        clamp: false,\n        offset: 0x00,\n        ..Default::default()\n    },\n    &mut dst,\n)?;"} python={"from autd3.units import Hz\nfrom autd3_modulation import (\n    FourierOption,\n    SineComponent,\n    SineOption,\n    fourier,\n    modulation_buffer,\n)\n\ndst = modulation_buffer()\n\nfourier(\n    [\n        SineComponent(\n            freq=100 * Hz,\n            option=SineOption(),\n        )\n    ],\n    FourierOption(\n        scale_factor=None,\n        clamp=False,\n        offset=0x00,\n    ),\n    dst,\n)"} csharp={"using AUTD3;\nusing static AUTD3.Units;\n\nvar dst = Modulation.ModulationBuffer();\n\nModulation.Fourier(\n    new[]\n    {\n        new SineComponent(\n            100 * Hz,\n            new SineOption()\n        ),\n    },\n    new FourierOption(\n        scaleFactor: null,\n        clamp: false,\n        offset: 0x00\n    ),\n    dst\n);"} />
