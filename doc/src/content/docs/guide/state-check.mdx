---
title: リンク状態の監視
description: State Checker でデバイスの EtherCAT リンク状態を監視する
sidebar:
  order: 6
---

import CodeTabs from "@components/CodeTabs.astro";
import { excerpt } from "@lib/excerpt";
import sample from "@codes/rust/examples/guide/state-check.rs?raw";
import pySample from "@codes/python/examples/guide/state-check.py?raw";
import csSample from "@codes/csharp/examples/guide/state-check.cs?raw";

`Checker`を使うことでデバイスとの EtherCAT リンクが正常に動いているかどうかを監視できる.

## Checker の取得

通常の [`Client::open`](/autd3-sdk/api/basics/client/) の代わりに `open_with_checker` を使うと, `Client` と `Checker` の組が返る.

<CodeTabs rust={excerpt(sample, "open")} python={excerpt(pySample, "open")} csharp={excerpt(csSample, "open")} />

## 状態の取得

`Checker::check` は, 呼び出した時点のリンク状態 `LinkStatus` を返す.
`LinkStatus` は以下を持つ.

- `devices`: デバイスごとの状態 (`DeviceState`). 正常なら `OP`, 切断されると `LOST` など.
- `all_op`: 全デバイスが `OP` 状態か.
- `any_lost`: いずれかのデバイスが `LOST` 状態か.
- `recoveries`: 切断から復帰した累計回数.

以下は, 一定間隔で `check` を呼び, 状態が変化したときだけ出力する例.

<CodeTabs rust={excerpt(sample, "poll")} python={excerpt(pySample, "poll")} csharp={excerpt(csSample, "poll")} />

`check` を呼ぶ頻度はユーザー次第である.
