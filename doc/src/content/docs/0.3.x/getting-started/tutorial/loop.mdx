---
title: ループと遷移
description: 有限ループ回数と, バンク切り替えタイミングの制御
sidebar:
  order: 8
slug: 0.3.x/getting-started/tutorial/loop
---

import CodeTabs from "@components/CodeTabs.astro";

`FociStm` / `PatternStm` は, デフォルトでは同じパターン列を無限に繰り返し再生する.
再生回数を有限にする場合は以下の通り.

## 有限ループ

再生回数は `FociStmOption` などの `loop_behavior` で指定する.
`LoopBehavior::Infinite` (デフォルト) は無限ループ, `LoopBehavior::Finite(n)` は `n` 回 (最大65535回) 再生して停止する.
なお, 停止時は最後のパターンが出力され続ける.

重要な点として, **有限ループは現在と異なるバンクへ切り替えたときに初めて機能する**.

<CodeTabs rust={"// Play the circular motion only 3 times, then stop.\n// A finite loop (and non-immediate transition) only fires when switching to a\n// different bank, so write to bank B1 instead of the current B0.\nlet mut builder = client.datagram_builder();\nbuilder.push(FociStm::new(\n    50.0 * Hz,\n    &foci,\n    FociStmOption {\n        loop_behavior: LoopBehavior::Finite(NonZeroU16::new(3).unwrap()),\n        bank: PatternBank::B1,\n        transition_mode: TransitionMode::SyncIdx,\n        ..Default::default()\n    },\n));\nfor frame in &builder.build()? {\n    client.send_checked(frame).await?;\n}"} python={"# Play the circular motion only 3 times, then stop.\n# A finite loop (and non-immediate transition) only fires when switching to a\n# different bank, so write to bank B1 instead of the current B0.\nbuilder = client.datagram_builder()\nbuilder.push(\n    FociStm(\n        50.0 * Hz,\n        foci,\n        FociStmOption(\n            loop_behavior=LoopBehavior.Finite(3),\n            bank=PatternBank.B1,\n            transition_mode=TransitionMode.SyncIdx,\n        ),\n    )\n)\nfor frame in builder.build():\n    await client.send_checked(frame)"} csharp={"// Play the circular motion only 3 times, then stop.\n// A finite loop (and non-immediate transition) only fires when switching to a\n// different bank, so write to bank B1 instead of the current B0.\nvar builder = client.DatagramBuilder();\nbuilder.Push(new FociStm(\n    50.0f * Hz,\n    foci,\n    new FociStmOption(\n        bank: PatternBank.B1,\n        loopBehavior: LoopBehavior.Finite(3),\n        transitionMode: TransitionMode.SyncIdx\n    )\n));\nforeach (var frame in builder.Build())\n{\n    await client.SendCheckedAsync(frame);\n}"} />

## 遷移タイミング

再生対象のバンクへ切り替えるタイミングは `transition_mode` で指定する.
使用できるモードは, 遷移先バンクのループ設定 (`loop_behavior`) によって制限される.

遷移先が無限ループのとき使用できるモード.

* `Immediate` (デフォルト): 書き込み後, 直ちにバンクを切り替える.
* `Ext`: 拡張モード. バンク内の再生が終わると次のバンクへ自動で切り替わる.

遷移先が有限ループのとき使用できるモード.

* `SyncIdx`: 切り替え対象バンクの再生インデックスが 0 に戻ったタイミングで切り替える.
* `SysTime(DcSysTime)`: 指定した [DC システム時刻](/autd3-sdk/0.3.x/api/basics/dc-sys-time/)に切り替える.
* `Gpio(GpioIn)`: 指定した [GPIO 入力ピン](/autd3-sdk/0.3.x/hardware/gpio)のトリガで切り替える.

:::note
ループと遷移の諸々の制約は複数のデバイスを同期させるために存在している.
:::

ループ設定と同様, 遷移タイミングもバンクを切り替えたときにのみ作用する.

あらかじめ別バンクへ次の出力を仕込んでおき, 任意のタイミングで切り替える, などの細かい制御を行う場合は, API リファレンスの [低レベルコマンド](/autd3-sdk/0.3.x/api/command/lowlevel/) を参照されたい.
